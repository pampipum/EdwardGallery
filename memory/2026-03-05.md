# 2026-03-05

## OpenClaw setup updates
- Installed and documented new skills: **qmd**, **agent-browser**, and **agentmail**.
- Added Agent Browser safety env defaults in config:
  - `AGENT_BROWSER_CONTENT_BOUNDARIES=1`
  - `AGENT_BROWSER_MAX_OUTPUT=50000`
  - `AGENT_BROWSER_DEFAULT_TIMEOUT=45000`
- Added QMD/llama env controls:
  - `NODE_LLAMA_CPP_GPU=off`
  - `NODE_LLAMA_CPP_LOG_LEVEL=error`

## Email automation
- `AGENTMAIL_API_KEY` was added to OpenClaw config and gateway restarted.
- AgentMail API auth confirmed working.
- Sent a real test email to **andremarqueslps@hotmail.com** via AgentMail (successful delivery response with message/thread IDs).

## QMD status
- Workspace collection indexed and embeddings run completed (local CPU run; slower but functional).
- Confirmed `qmd search` works reliably; `qmd query` is heavier and can be noisy on this VPS.

## Ops and model config
- Gateway was restarted multiple times on request during troubleshooting.
- Added model alias **5.4** (`openai-codex/gpt-5.4`) to `agents.defaults.models` and restarted successfully.

## GitHub repo creation path
- `gh` CLI installation was blocked by elevated policy in-session.
- Added fallback script for repo creation via GitHub API:
  - `scripts/create-github-repo.sh`
- Added runbook:
  - `docs/ops/github-repos.md`
