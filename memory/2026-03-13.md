
## 2026-03-13 durable notes
- Upgraded OpenClaw from 2026.3.8 to 2026.3.11 on user request. Highlighted relevant changes: security fix for browser-origin WebSocket validation, ACP `resumeSessionId`, Discord auto-thread archive duration config, improved Discord reply chunking, memory/Gemini indexing improvements, and stricter cron delivery behavior.
- Diagnosed ACP/Codex failures: stale `codex-acp` helper processes were consuming ACP runtime slots; after clearing that blockage, direct ACP repro still failed due to Codex quota/billing exhaustion (`UsageLimitExceeded`). Gemini ACP also was not healthy as a fallback at that time.
- Established Discord project-channel workflow: create channel, wire Discord/OpenClaw bindings, and use `docs/project-channel-workflow.md` plus `docs/projects/<project-slug>.md` with standard update format `Status / Blockers / Next steps`.
- Created/wired project channels for `website-redesign`, `landing-page-redesign`, `alphaarena-migration`, and `ai-audits` so Albi can respond there.
- AI Audits cron audit finding: scheduler healthy overall, but there were no AI Audits-specific cron jobs; passed this finding and a suggested internal-only cron plan into the AI Audits channel for pickup.
- Added `ai-audits/.gitignore` with standard ignores for editor junk, Python caches/venvs, env files, node_modules, logs, temp/build output.
- Fixed GitHub Push Protection block in workspace repo: `alphaarena-migration/.env` containing multiple secrets had been committed in the workspace git repo. Removed it from tracking, added ignore rule in workspace `.gitignore`, amended the latest commit, and successfully pushed to `origin/main`. Follow-up needed: rotate exposed secrets from that `.env` (OpenAI, Gemini, Gmail app password, and other API keys).
